# SQL Tests — Foundation v1

Minimal checks to validate the star schema and KPI layer.
**Expected outcome:** tests return **0** (no issues) or **OK** for coverage.

> Run these in MariaDB (DBeaver) after building dims/facts/views.

---

## T1 — Uniqueness

### dim_store (no duplicate store_code)
```sql
SELECT COUNT(*) - COUNT(DISTINCT store_code) AS dup_store_code
FROM dim_store;
